# Text to Audio Converter

### This python program extract text file from a webpage and download as audio file, and play the audio file too.

## How to run the project
* Clone this repository
* Create a new environment 
                <pre><code>conda create -n newenv python==3.6</code></pre>
* Open any IDE
* Install all the required library run the app by <pre><code>main.py</code></pre>

After executing the code it will take a while to extract and download the text and play it automatically
